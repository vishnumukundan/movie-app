// ignore_for_file: camel_case_types

import 'package:flutter/material.dart';

class Snackbar__widget extends StatelessWidget {
  const Snackbar__widget({Key? key}) : super(key: key);
  @override
  Widget build(BuildContext context) {
    return const SizedBox(
        // child: Get.snackbar('Not implemented', ''),
        );
  }
}
